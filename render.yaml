services:
  - type: web
    name: roadygo-web
    runtime: static
    buildCommand: ./scripts/render-build.sh
    staticPublishPath: build/web
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
